const Layout = ({
  childern,
  isThemeDialogOpen,
  toggleThemeDialog,
  isStatusPreviewOpen,
  statusPreviewContent,
}) => {
  return (
    <div>
      <div>layout</div>
    </div>
  );
};

export default Layout;
